# Code-OSS 1.133.0 → 1.134.0 Upstream Change Analysis

**Migration Target**: Code-OSS / VS Code `1.134.0` (Commit `b6a2e4c89283fa858bf4e3752e3794711e592671`)

---

## 1. Domain Change Summary & Categorization

| Domain / Subsystem | Upstream 1.134.0 Key Changes | Falkon Classification | Integration Impact |
| :--- | :--- | :--- | :--- |
| **Chat / Subagents** | Side-by-side chat view, prompt timeline history, find-in-chat widget, long conversation virtualized navigation. | `UPSTREAM-CLEAN` | Inherited automatically via webview workbench bundle. |
| **Workbench & UI** | Updated tab bar rendering, enhanced activity bar layout options, custom titlebar control center defaults. | `UPSTREAM-CLEAN` | Inherited automatically. |
| **Extension Host** | `extensionHostProcess.ts` protocol framing (`IPCExtHostConnection` & `VSCODE_EXTHOST_IPC_HOOK`) remain backward-compatible with 1.133.0. | `UPSTREAM-CLEAN` | No protocol breaking changes detected. |
| **Platform (`platform.ts`)** | `navigator` global deprecation warnings & Touch ID checks updated. | `UPSTREAM-PATCHED` | Patch `FALKON-001` flips `_isNative=true`, `_isWeb=false` cleanly. |
| **Extension Scanner** | `builtinExtensionsScannerService.ts` static web gate logic. | `UPSTREAM-PATCHED` | Patches `FALKON-003` to `FALKON-008` bypass gate and cache promises. |
| **Lifecycle Service** | `lifecycleService.ts` beforeUnload listener. | `UPSTREAM-PATCHED` | Patch `FALKON-002` suppresses browser unload dialog freeze. |
| **Workbench Injection** | `workbench.ts` `configurationDefaults` setup. | `UPSTREAM-PATCHED` | Patches `FALKON-009` & `FALKON-010` inject titlebar & color defaults. |
| **Filesystem Provider** | File operations delegation (`src/vs/platform/files/browser/tauriFileSystemProvider.ts`). | `UPSTREAM-REPLACED` | Uses `TauriFileSystemProvider` and Rust security layer. |
| **Terminal Backend** | Portable PTY backend bridge (`src/vs/workbench/services/terminal/browser/tauriTerminalBackend.ts`). | `UPSTREAM-REPLACED` | Uses Rust `portable-pty` session manager. |

---

## 2. Specific Feature Parity Matrix (1.134.0 Capabilities)

1. **Side-by-Side Multi-Pane Chats**: `UPSTREAM-CLEAN` (Automatically supported in workbench bundle).
2. **Subagent Prompt Timeline**: `UPSTREAM-CLEAN` (Automatically supported).
3. **Find Text Within Chat**: `UPSTREAM-CLEAN` (Automatically supported).
4. **Long Conversation Navigation**: `UPSTREAM-CLEAN` (Virtual scroll list rendered in workbench).
5. **Node.js Environment & Security**: `UPSTREAM-CLEAN` (Locked to Node.js v24 runtime with `ELECTRON_RUN_AS_NODE=1`).
